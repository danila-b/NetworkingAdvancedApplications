apiVersion: v1
kind: Service
metadata:
  name: products
  namespace: default
  labels:
    app: products
spec:
  selector:
    app: products
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 32001
      name: http
  type: NodePort